[pytest]
markers =
    api_vertical: end-to-end API workflow tests that exercise several application layers
    migration: SQLite migration, repair, and downgrade compatibility tests
    postgresql: tests that require the disposable PostgreSQL CI service
