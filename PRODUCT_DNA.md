# Slow 产品底层基因

Slow 交付的不是学习计划，而是学习者可以逐节学习、验证并持续积累记忆的个性化教材。以下层级是产品、生成、界面、进度与证据共同遵守的领域契约。

```text
用户 → 书架 → 系列（学习目标） → 书 → 章 → 节
节内结构：内容块
```

## 每一层只回答一个问题

- **书架**：这是哪个长期学科、领域或专业？
- **系列**：为了完成这个已确认的学习目标，需要按什么顺序学习哪些书？
- **书**：这一本围绕哪个完整学习主题展开？
- **章**：哪些相关知识点组成一个阶段性的知识聚合？
- **节**：这次以哪个核心知识点为学习锚点，需要调用哪些关联知识才能真正理解它？
- **内容块**：用什么教学步骤讲清这一节，例如定义、机制、例子、边界、练习或小结？

## 不可降级的粒度规则

- 每个已确认学习目标形成一个系列；系列组织同一书架内为该目标服务的有序书籍，并承载目标级进度。
- 一本书必须拥有完整、可命名的主题和多个章节，不能只是一个章节的包装或别名。
- 一章是一组相关知识点的聚合，通常对应约一天并包含 3–5 节；章不能退化成一个 15–20 分钟即可学完的单一知识点。
- 一节只有一个核心知识点和主要验证问题，典型投入为 15–20 分钟，但它不是孤立知识节点。正文可以按因果依赖调用前置、机制、对比、边界、应用与迁移等关联知识。
- 关联知识的展开由学习证据决定：已经稳固的只做必要连接，薄弱或缺失的应补充脚手架；知识完整性优先，不得为了凑时长机械拆分。
- 如果某个前置缺口无法在不新增并列考核目标、且不破坏本节典型投入的情况下补足，必须退出正文生成并触发显式重规划，不能把多个核心知识点塞进当前小节。
- 内容块没有独立导航、进度或解锁状态。“定义、历史、案例、小结、自测”等教学阶段不能仅凭形式被生成为并列小节。
- 支撑性关联知识不得静默变成新的并列考核目标。只有在正文生成前已经写入并版本化于当前 Learning Contract 的目标才能进入测验并形成掌握证据；其余内容只承担理解当前核心知识点所需的脚手架作用。

## 正文生成与证据边界

- 正文和测验默认由一次模型调用共同生成，模型结果只是候选内容。
- 服务端确定性校验只保证 Schema、契约、绑定和发布边界，不得表述为事实已经核验。
- 契约外知识进入正文块的可考核目标绑定或进入测验时，整个候选结果必须在正式持久化之前失败；禁止静默忽略、猜测映射或自动修正。
- 只有通过全部门禁并原子发布的正式内容版本，才能进入阅读、测验、解锁和学习证据链。
- AI、Demo 与权利已确认知识底座等生成模式必须机器可识别并向用户作必要说明。

详细架构、错误码、状态机和实施顺序见 [ADR-0001：小节正文与测验的一次生成及原子发布](docs/decisions/0001-lesson-generation-v2.md)。

## 编号是领域语义

- 书显示独立书序号，例如“第 1 本”。
- 章在每本书内显示为“第 1 章、第 2 章……”。
- 节显示为“章序号.节序号”，例如“1.1、1.2、2.1”。
- 不得把书序号与章序号拼成 `1.1`，再把真正的小节显示成裸数字。

目录、面包屑、AI 生成提示词、服务端状态、测验、解锁和学习证据必须使用同一套定义。任何功能如果需要重新解释这些层级，说明实现已经偏离产品模型。
